import React from 'react';

interface Props {
    id: string;
}

const TextInputField: React.FC<Props> = (props) => {
    //
    return (
        //
        <button {...props}>
            Click ME PLEASE
        </button>
    )
};

export default TextInputField